/* PROT CARD — Supabase connection config.
   Fill in your self-hosted Supabase URL and anon (public) key.
   Both the public site and /admin read from this file. */
window.PC_SUPABASE = {
  url:  "",   // e.g. "https://supabase.tudominio.com"
  key:  ""    // anon (public) key — safe to expose in the browser
};
